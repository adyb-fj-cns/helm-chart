apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bluegreen2.fullname" . }}-test-connection"
  labels:
{{ include "bluegreen2.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bluegreen2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
